# FlightRadar24 official Python SDK
fr24>=0.3.0

# HTTP client (dependency of fr24, but listed explicitly)
httpx[http2]
h2>=4.0

# Environment variable management
python-dotenv>=1.0.0

# Web interface
flask
folium

# Protobuf (dependency of fr24)
protobuf
grpcio

# Maps, images, HTTP utilities
Pillow
requests

# ISS pass prediction (local TLE compute — no external API)
ephem>=4.1

# ATC audio — Chromecast + AirPlay output backends.
# Optional: imported lazily, and the ATC feature degrades to local USB/onboard
# output when they are absent. Install them to enable casting ATC to a
# Chromecast/Google speaker or an AirPlay receiver.
pychromecast>=14.0
pyatv>=0.16.0
